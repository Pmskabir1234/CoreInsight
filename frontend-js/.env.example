# Development: leave empty — Vite proxy handles routing to http://127.0.0.1:8000
# VITE_API_URLS=

# Production: comma-separated list of backend instances for client-side load balancing
# VITE_API_URLS=http://api1.example.com,http://api2.example.com,http://api3.example.com
